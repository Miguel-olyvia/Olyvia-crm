# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files
.env
.env.local
.env.*.local

# Local AI/workspace tooling (never publish)
.claude/
.codex/
.gitnexus/
.tmp/
AGENTS.md
CLAUDE.md
task-done.ps1

# Documentation lives outside the Git repository in ../vault
docs/
# ...menos os do modulo de Operacoes, que fazem parte dele e sao ligados
# a partir do operacao-app/README.md.
!operacao-app/docs/
ficheiros/

.env
src.zip
supabase/.temp/
supabase/.branches/
supabase/seed.sql
migrations_disabled_local/
.vercel
.env*

# Screenshots e dumps de sessoes de teste que aterram na raiz do repo
/*.png
/_hdr*.txt
/_body*.json
.mcp.json

# Playwright artefactos e estado de sessao (contem tokens) — nunca commitar
test-results/
playwright-report/
tests/.auth/

# Local secrets
env

# Arquivado no vault a 2026-08-31 (vault/ficheiros/arquivo-repositorio/).
# Fora do repositorio, mas nao apagado: codigo morto que ninguem importa,
# lockfiles de um gestor que o projecto nao usa, e um zip do proprio src.
eliminar/
eliminar.md
.prewarm
bun.lock
bun.lockb
src (2).zip
